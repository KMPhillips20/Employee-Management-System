const Intern = require("../lib/Intern");

describe("Interns are still in school", () => {
    const intern = new Intern('Kyle Phillips', 1023, 'kylephillips65@yahoo.com', 'NC State');

    expect(engineer.gitHub).toEqual(expect.any(String));

});
